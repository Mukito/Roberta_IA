# Inicio

Cria-se um MV
`python -m venv venv` <br>
`/activate` <br>

### Bibliotecas
pypi.org <br>

SpeechRecognition 
`pip install SpeechRecognition`
<br>

pyttsx3
`pip install pyttsx3`
<br>


pyaudio
`pip install PyAudio`
<br>
